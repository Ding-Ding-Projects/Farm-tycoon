// audio.js — WebAudio SFX synthesized in code (no audio files): pops, coin clinks,
// harvest swishes, ambient birdsong. Respects state.settings.sound.

/** Lazy-init the AudioContext on first user gesture (browser autoplay policy). */
export function init() { /* Phase B */ }

/** Play a named SFX: 'pop'|'coin'|'harvest'|'plant'|'levelup'|'splash'|'dig'|'error'. */
export function sfx(name) { /* Phase B */ }

/** Start/stop the gentle ambient loop (birds, breeze). */
export function ambient(on) { /* Phase B */ }
