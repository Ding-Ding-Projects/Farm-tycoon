// mine.js — the mine (L24): spend a pickaxe or dynamite to dig; yields ore/gems per
// MINE.tools weight tables (event gold_rush doubles). Ore refines in the Smelter building
// (a normal production building — see production.js/enqueue).

/** Dig with a tool ('pickaxe'|'dynamite'); consumes it, rolls yields into the barn. */
export function dig(tool) { /* Phase B */ }

/** Tools the player currently owns (for the mine panel UI). */
export function availableTools() { /* Phase B */ }
